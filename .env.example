# Copy this to `.env` and fill it in before running `docker compose up`.

# Generate with: mix phx.gen.secret
SECRET_KEY_BASE=

# Host the app is served from (used to build URLs, cookies, etc.)
PHX_HOST=localhost
